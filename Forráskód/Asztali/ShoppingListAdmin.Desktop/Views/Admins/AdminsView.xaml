<UserControl x:Class="ShoppingListAdmin.Desktop.Views.Admins.AdminsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ShoppingListAdmin.Desktop.Views.Admins" 
             xmlns:users="clr-namespace:ShoppingListAdmin.Desktop.Views.Users"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="35"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <!-- Felhasználók menü -->
        <users:UsersMenu Grid.Row="0"></users:UsersMenu>

        <!-- Szűrő panel -->
        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="1">
            <TextBox x:Name="FilterUserName" Width="150" Margin="5" VerticalAlignment="Center"/>
            <Button Content="Szűrés" Width="75" Height="30" Margin="5" VerticalAlignment="Center" Click="FilterButton_Click"/>
        </StackPanel>
        <DataGrid x:Name="AdminDataGrid" AutoGenerateColumns="False" 
                  CanUserAddRows="False" CanUserDeleteRows="False" SelectionMode="Single" Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*"/>
                <DataGridTextColumn Header="Felhasználónév" Binding="{Binding UserName}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="JELSZÓ" Binding="{Binding PasswordHash}" Width="*"/>
                <DataGridTemplateColumn Header="Műveletek" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Törlés" Width="50" Margin="5" Click="DeleteAdmin_Click"/>
                                <Button Content="Módosítás" Width="70" Margin="5" Click="EditAdmin_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
