<UserControl x:Class="ShoppingListAdmin.Desktop.Views.Settings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:ShoppingListAdmin.Desktop.ViewModels.Settings"
            d:DataContext="{d:DesignInstance Type=viewModels:SettingsViewModel, IsDesignTimeCreatable=True}"
             xmlns:local="clr-namespace:ShoppingListAdmin.Desktop.Views.Settings"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Beállítások" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
            
            <CheckBox Content="Sötét téma" IsChecked="{Binding IsDarkTheme}" Margin="0,10"/>
            <CheckBox Content="Értesítések engedélyezése" IsChecked="{Binding NotificationsEnabled}" Margin="0,10"/>
            <CheckBox Content="Hang engedélyezése" IsChecked="{Binding SoundEnabled}" Margin="0,10"/>
            
            <TextBlock Text="Betűméret:" Margin="0,20,0,5"/>
            <ComboBox SelectedItem="{Binding FontSize}" Margin="0,0,0,20">
                <ComboBoxItem Content="Kicsi" />
                <ComboBoxItem Content="Közepes" />
                <ComboBoxItem Content="Nagy" />
            </ComboBox>
            
            <Button Content="Mentés" Command="{Binding SaveSettingsCommand}" Width="100" HorizontalAlignment="Left"/>
        </StackPanel>
    </Grid>
</UserControl>