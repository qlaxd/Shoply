<UserControl x:Class="ShoppingListAdmin.Desktop.Views.Statistics.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShoppingListAdmin.Desktop.Views.Statistics"
             xmlns:viewModel="clr-namespace:ShoppingListAdmin.Desktop.ViewModels.Statistics"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             d:DataContext="{d:DesignInstance Type=viewModel:StatisticsViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Foreground="White">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Statisztika" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Main Content Section -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- User Statistics -->
            <StackPanel Grid.Column="0" Margin="10">
                <TextBlock Text="Felhasználói statisztika" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>
                
                <TextBlock Text="Összes felhasználó:" FontWeight="Bold" Margin="0,5"/>
                <TextBlock Text="{Binding TotalUsers}" FontSize="16" Margin="0,0,0,10"/>

                <TextBlock Text="Aktív felhasználók:" FontWeight="Bold" Margin="0,5"/>
                <TextBlock Text="{Binding ActiveUsers}" FontSize="16" Margin="0,0,0,10"/>

                <TextBlock Text="Új felhasználók ebben a hónapban:" FontWeight="Bold" Margin="0,5"/>
                <TextBlock Text="{Binding NewUsersThisMonth}" FontSize="16" Margin="0,0,0,10"/>

                <TextBlock Text="Lista statisztika" FontWeight="Bold" FontSize="18" Margin="0,10,0,10"/>
                
                <TextBlock Text="Összes lista:" FontWeight="Bold" Margin="0,5"/>
                <TextBlock Text="{Binding TotalLists}" FontSize="16" Margin="0,0,0,10"/>

                <TextBlock Text="Aktív listák:" FontWeight="Bold" Margin="0,5"/>
                <TextBlock Text="{Binding ActiveLists}" FontSize="16" Margin="0,0,0,10"/>

                <TextBlock Text="Teljesített listák:" FontWeight="Bold" Margin="0,5"/>
                <TextBlock Text="{Binding CompletedLists}" FontSize="16" Margin="0,0,0,10"/>

                <TextBlock Text="Átlagos listák száma felhasználónként:" FontWeight="Bold" Margin="0,5"/>
                <TextBlock Text="{Binding AverageListsPerUser, StringFormat=N2}" FontSize="16" Margin="0,0,0,10"/>
            </StackPanel>

            <!-- Product and Collaboration Statistics -->
            <StackPanel Grid.Column="1" Margin="10">
                <TextBlock Text="Termék statisztika" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>
                
                <TextBlock Text="Összes termék:" FontWeight="Bold" Margin="0,5"/>
                <TextBlock Text="{Binding TotalProducts}" FontSize="16" Margin="0,0,0,10"/>

                <TextBlock Text="Áltagos termékszám listánként:" FontWeight="Bold" Margin="0,5"/>
                <TextBlock Text="{Binding AverageProductsPerList, StringFormat=N2}" FontSize="16" Margin="0,0,0,10"/>

                <TextBlock Text="Legtöbbet hozzáadott termék" FontWeight="Bold" FontSize="16" Margin="0,10,0,5"/>
                <DataGrid ItemsSource="{Binding MostAddedProducts}" 
                          AutoGenerateColumns="False" 
                          Height="150" 
                          Margin="0,0,0,10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Termék" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn Header="Darabszám" Binding="{Binding Count}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Text="Együttműködési statisztika" FontWeight="Bold" FontSize="18" Margin="0,10,0,10"/>
                
                <TextBlock Text="Átlagos hozzájárulás felhasználónként:" FontWeight="Bold" Margin="0,5"/>
                <TextBlock Text="{Binding AverageContributorsPerList, StringFormat=N2}" FontSize="16" Margin="0,0,0,10"/>

                <TextBlock Text="Közös listák:" FontWeight="Bold" Margin="0,5"/>
                <TextBlock Text="{Binding CollaborativeListsPercentage, StringFormat=N2}" FontSize="16" Margin="0,0,0,10"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
