<UserControl x:Class="ShoppingListAdmin.Desktop.Views.Categories.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:ShoppingListAdmin.Desktop.ViewModels.Categories"
             d:DataContext="{d:DesignInstance Type=viewModels:CategoryViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/UIColors.xaml"/>
                <ResourceDictionary Source="/Styles/DataGridStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <!-- Loading Indicator -->
        <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="{StaticResource backgroundPrimaryColor1}" Opacity="0.7"/>
            <ProgressBar IsIndeterminate="True" 
                         Height="50" 
                         Width="200"
                         Foreground="{StaticResource colorMidleGreen}"/>
        </Grid>

        <!-- Error Message -->
        <TextBlock Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   Margin="0,10,0,0"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <!-- Main Content -->
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Kategóriák" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           VerticalAlignment="Center"
                           Foreground="White"/>
                
                <Button Content="Új kategória"
                        Command="{Binding CreateCategoryCommand}"
                        Style="{StaticResource ActionButtonStyle}"
                        Margin="20,0,0,0"/>
            </StackPanel>

            <!-- Categories DataGrid -->
            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding Categories}"
                      SelectedItem="{Binding SelectedCategory}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="False"
                      Style="{StaticResource DataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" 
                                      Binding="{Binding Id}"
                                      IsReadOnly="True"
                                      Width="*"/>
                    <DataGridTextColumn Header="Név" 
                                      Binding="{Binding Name}"
                                      Width="*"/>
                    <DataGridTextColumn Header="Leírás" 
                                      Binding="{Binding Description}"
                                      Width="*"/>
                    <DataGridTextColumn Header="Szülő kategória" 
                                      Binding="{Binding ParentCategoryId}"
                                      Width="*"/>
                    <DataGridTextColumn Header="Szint" 
                                      Binding="{Binding Level}"
                                      Width="*"/>
                    <DataGridCheckBoxColumn Header="Aktív" 
                                          Binding="{Binding IsActive}"
                                          Width="*"/>
                    <DataGridTextColumn Header="Létrehozva" 
                                      Binding="{Binding CreatedAt, StringFormat={}{0:yyyy.MM.dd HH:mm}}"
                                      Width="*"/>
                    <DataGridTextColumn Header="Frissítve" 
                                      Binding="{Binding UpdatedAt, StringFormat={}{0:yyyy.MM.dd HH:mm}}"
                                      Width="*"/>
                    <DataGridTemplateColumn Header="Műveletek" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Szerkesztés"
                                            Command="{Binding DataContext.UpdateCategoryCommand, 
                                                      RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource IconButtonStyle}"
                                            Margin="0,0,5,0"/>
                                    <Button Content="Törlés"
                                            Command="{Binding DataContext.DeleteCategoryCommand, 
                                                      RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource IconButtonStyle}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>